name: AWS CLI usage

parameters:
  - name: flavor
    type: string

jobs:
  - job: test
    pool:
      name: github-actions
      demands:
        - flavor_${{ parameters.flavor }}
    steps:
      - bash: |
          aws --version
        displayName: Test AWS CLI installation
