name: Python usage

parameters:
  - name: flavor
    type: string

jobs:
  - job: test
    pool:
      name: github-actions
      demands:
        - flavor_${{ parameters.flavor }}
    steps:
      - bash: |
          python --version | grep -q "^3."
        displayName: Test Python installation

      - bash: |
          python -c "print('Hello world')" | grep -q "^Hello world$"
        displayName: Test Python script
